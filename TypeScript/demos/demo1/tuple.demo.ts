// declare tuple with known types
// create a tuple with two elements, where first one is a string, and the second one is a number.
let tup: [string, number];
tup = ["string", 123];
// tup = [123, "string"]; // error, as the order is incorrect
// tup = ["string", "another string"]; // error, types not matched
