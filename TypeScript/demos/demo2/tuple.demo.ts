// Tuple

// declare a tuple with two elements, where first one is a string, and the second is a number.
let row: [string, number];

row = ["curtis", 123];
console.log(row[0], row[1]);

row[0] = "banana";
row[1] = 456;
console.log(row);
