/**
 * Compute a to the power of b using the Math.pow() method.
 */
public class ComputePower {

    public static void main(String[] args) {

        double a, b;
        a = 2;
        b = 3;
        System.out.println(a + " to the power of " + b + " = " + Math.pow(a, b));
    }
}